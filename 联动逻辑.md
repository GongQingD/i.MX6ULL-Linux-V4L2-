# 联动逻辑

## 维护说明

- 本文件放在仓库根目录，作为多传感器联动逻辑的单一中文说明入口。
- 当前阶段先记录“已确认的联动设计”和“当前实现边界”，避免把旧版本里出过问题的方案重新混回去。
- 后续如果继续扩展联动逻辑，应直接在本文件追加新章节，而不是分散写到聊天记录里。
- 如果后续方案覆盖了当前章节，需要显式写清楚被覆盖内容和覆盖原因，例如稳定性修正、交互简化、重新拆阶段实现。

## 当前基线

- 当前稳定基线是 `My_Project` 主界面可以正常显示传感器数据，并可手动启动 `Camera_Project`。
- `SR501` 当前保留异步通知机制：
  - `My_Project` 启动后对 `/dev/sr501` 配置 `F_SETOWN + O_ASYNC`
  - 驱动通过 `kill_fasync(SIGIO)` 发送通知
  - Qt 侧通过 `socketpair + QSocketNotifier` 把 `SIGIO` 转进事件循环
- `AP3216C` 与 `DHT11` 当前仍通过 `QTimer` 定时读取。
- 当前阶段不直接恢复之前那套完整的 `SensorWorker/QThread + 自动接管 + 多层 ownership` 方案，因为那套方案在板端叠加后排障面过大。

## 第一阶段目标

- 在当前稳定基线之上，重新打开两条自动动作：
  - `SR501 -> Camera_Project`
  - `AP3216C -> LED`
- 第一阶段目标是先把“检测到传感器变化后执行自动动作”重新做通。
- 第一阶段明确不做：
  - 不恢复 `SensorWorker/QThread`
  - 不恢复旧版本里复杂的自动/手动 ownership 切换
  - 不新增 UI 控件
  - 不改 `Camera_Project` 主链路

## 第一阶段联动方案

### 1. SR501 -> Camera_Project

- `SR501` 继续使用当前已经稳定的异步通知机制，不改回轮询方案。
- 当 Qt 侧收到 `SR501` 通知并读到“有人”状态时：
  - 如果 `Camera_Project` 当前未运行，则自动启动
  - 刷新自动关闭截止时间为“当前时刻 + 5 秒”
- 当 Qt 侧读到“无人”状态时：
  - 不立刻关闭 `Camera_Project`
  - 由主界面现有定时器统一检查是否已经超过 5 秒保持时间
- 当满足以下条件时，自动关闭 `Camera_Project`：
  - 当前相机会话是由 `SR501` 自动拉起
  - 当前状态已无人
  - 当前时刻已经超过最后一次人体触发后的 5 秒截止时间

### 2. AP3216C -> LED

- `AP3216C` 继续通过当前主界面定时器读取，不在第一阶段引入 worker thread。
- 每次读取到光照值后，除了刷新 UI，还同时执行自动控灯判断。
- 自动控灯采用迟滞阈值：
  - 光照低于暗阈值时自动开灯
  - 光照高于亮阈值时自动关灯
- 使用双阈值而不是单阈值，避免光照值在边界附近波动时 LED 反复开关。

## 第一阶段状态变量

- `cameraAutoRunning`
  - 标记当前 `Camera_Project` 是否是由 `SR501` 自动拉起
- `cameraAutoDeadlineMs`
  - 记录最近一次人体触发后的自动关闭截止时间
- `lastSr501State`
  - 记录最近一次读取到的 `SR501` 状态，用于区分“有人/无人”
- `ledAutoState`
  - 记录自动控灯逻辑下当前认为的 LED 状态，避免重复写设备

## 第一阶段数据流

### SR501 异步链

- 驱动触发 `SIGIO`
- Qt 侧收到通知
- `My_Project` 读取 `/dev/sr501`
- 如果状态为 `1`：
  - 启动或续期自动相机会话
- 如果状态为 `0`：
  - 只更新状态，不立即关闭相机

### 主界面定时器链

- 定时读取 `AP3216C`
- 定时读取 `DHT11`
- 刷新界面数值
- 同时执行：
  - `AP3216C -> LED` 自动控灯判断
  - `SR501` 自动相机会话是否到期的判断

## 当前建议的实现顺序

1. 先在 `My_Project/mainwindow.cpp` 内把自动动作重新打开，不引入 `SensorWorker/QThread`
2. 先验证 `SR501 -> Camera_Project` 自动启动、5 秒续期、无人超时关闭
3. 再验证 `AP3216C -> LED` 暗光开灯、亮光关灯
4. 第一阶段稳定后，再决定是否进入第二阶段，把联动状态机重新抽回纯逻辑模块

## 第一阶段验收结果

- 板端现场验收结果：
  - `SR501`: 自动打开 `Camera_Project`，并在无人后约 `5 秒` 自动关闭
  - `AP3216C`: 遮挡后自动亮灯，移开后自动灭灯
- 因此当前可以确认：
  - 第一阶段最小可用联动方案已经在板端跑通
  - 当前可用基线是“`SR501` 异步通知 + `AP3216C` 定时器 + `MainWindow` 最小状态机”
- 当前仍未纳入第一阶段的内容：
  - `SensorWorker/QThread`
  - 自动/手动会话 ownership
  - 相机内交互接管逻辑

## 第二阶段预留

- 如果第一阶段板端稳定，再考虑把联动判断从 `MainWindow` 抽到独立状态机模块。
- 如果第一阶段稳定，再考虑是否恢复 `SensorWorker/QThread`，但要以“不会破坏当前稳定启动链”为前提。
- 如果后续重新引入复杂交互，例如“用户在 `Camera_Project` 中操作后升级为手动会话”，需要单独新增章节，不直接覆盖第一阶段最小可用方案。

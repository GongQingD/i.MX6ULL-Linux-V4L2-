# 项目更新日志

## 维护规则

- 本文件放在仓库根目录，作为项目功能演进的单一中文日志入口。
- 每次新增功能或调整方案，都新增一个独立章节，标题格式统一为 `## YYYY-MM-DD 更新 N：标题`。
- 每个更新章节至少写清楚 5 件事：变更内容、直接好处、影响范围、当前状态、覆盖关系。
- 如果后续更新覆盖了之前的更新，必须在新章节里显式引用被覆盖章节，并写清楚覆盖原因，例如项目设计调整、性能优化、稳定性修复、交互简化、硬件约束变化。
- 如果某次更新仍处于方案阶段，必须明确写 `状态：方案已确认，尚未实现`，不能写成“已完成”。

## 2026-03-22 更新 001：多传感器联动与多线程控制方案

### 变更内容

- 引入多传感器联动方案：
  - `SR501 -> 摄像头`：检测到人时自动启动 `Camera_Project`。
  - `AP3216C -> LED`：环境变暗时自动开灯，变亮时自动关灯。
- 联动控制从“主线程里分散轮询 + 手动按钮触发”收束为“状态机统一决策”。
- 状态机设计采用低复杂度 `if/else` 方案，不引入复杂状态机框架。
- 计划把传感器采样和联动判断放入独立 worker thread，主线程只负责 UI、`QProcess` 摄像头控制、`/dev/led` 写操作。
- `SR501` 自动打开摄像头后的保持时间定为 `5 秒`；5 秒内如果再次检测到人，则自动续期。
- `AP3216C` 控灯将采用迟滞阈值，避免在临界亮度附近反复开关。
- 手动操作和自动联动会分开管理：
  - 手动启动的摄像头不受自动 5 秒超时关闭影响。
  - 手动切换 LED 后，自动控灯会暂时冻结，避免刚手动改完就被自动逻辑改回去。

### 直接好处

- 对用户体验更直接：
  - 人来了自动看监控，不需要先点按钮。
  - 天暗了自动亮灯，交互更接近真实“智能家居”。
- 对系统行为更稳定：
  - AP3216C 加迟滞后，不会因为光照值在阈值附近波动就让 LED 来回闪。
  - 自动联动和手动操作分开后，不会出现“用户刚操作，系统立刻打回去”的冲突。
- 对工程结构更清晰：
  - 传感器采样、状态判断、动作执行边界明确，后续继续加联动时不容易把 `MainWindow` 堆成大杂烩。
- 对界面响应更友好：
  - 传感器读取和联动判断搬到 worker thread 后，主线程不用再同时承担 UI 刷新、设备读取和流程判断，界面卡顿风险更低。
- 对后续测试更有利：
  - 状态机拆出来后，可以先在主机侧验证联动逻辑，再上板做设备联调。

### 影响范围

- 设计主入口：`My_Project`
- 计划新增或调整的核心模块：
  - `SensorWorker`
  - `LinkageLogic`
  - `MainWindow` 中的摄像头与 LED 动作接口
- 当前不计划改动的模块：
  - `Camera_Project` 的视频采集与显示主链路
  - `SR501`、`AP3216C`、`LED` 的内核驱动实现

### 当前状态

- 状态：方案已确认，尚未实现
- 当前仓库现状仍然是：
  - `My_Project` 主线程定时读取 `AP3216C` 和 `SR501`
  - 摄像头仍然通过按钮手动启动
  - 多传感器自动联动和 worker thread 目前还没有落到业务代码中
- 本次方案对应的实现计划已写入：`docs/plans/2026-03-22-sensor-linkage.md`

### 覆盖关系

- 无
- 说明：这是该日志的第一条记录，后续如果出现“联动规则调整”“5 秒保持时间改动”“单线程改回主线程”“阈值策略变化”等更新，必须在新章节中引用本节，并写清楚覆盖原因。

## 2026-03-23 更新 002：多传感器联动与多线程实现落地

### 变更内容

- 在 `My_Project` 中新增纯 C++ 联动逻辑模块，把 `SR501 -> 摄像头` 和 `AP3216C -> LED` 的规则从界面代码里拆出来，形成可独立测试的状态机。
- 新增 `SensorWorker`，把 AP3216C、DHT11、SR501 的采样搬到独立 worker thread，主线程不再直接轮询这些传感器。
- `SR501` 联动已按当前方案接入：
  - 检测到人时自动启动 `Camera_Project`
  - 最后一次人体触发结束后保留 `5 秒`
  - 只有自动会话才会被这条超时规则自动关闭
- `AP3216C` 联动已按当前方案接入：
  - 环境变暗自动开灯
  - 环境变亮自动关灯
  - 使用迟滞阈值避免临界抖动
- 手动覆盖规则已接入：
  - 手动启动摄像头不会被自动 5 秒超时关闭
  - 手动切换 LED 后自动控灯冻结 `60 秒`
- 主线程开始真正消费联动决策：
  - 自动 LED 动作通过 `/dev/led` 执行
  - 自动摄像头动作通过 `QProcess` 拉起或停止 `Camera_Project`
- 为降低状态漂移风险，增加了最小回滚路径：
  - 自动启动摄像头失败时，回滚 worker 内部 camera 状态
  - 自动控灯写失败时，尽量把 worker 内部 LED 状态回滚到实际值

### 直接好处

- 交互从“查看传感器数据”升级成“真正有动作的联动系统”，项目完成度明显更高。
- 纯逻辑状态机独立出来后，可以先在主机侧用 `g++` 做红绿验证，再上板做设备联调，减少板端反复试错。
- 传感器采样搬到 worker thread 后，UI 主线程不再承担同步轮询压力，后续继续加规则时不容易把界面线程拖卡。
- 手动覆盖规则真正落地后，系统不会和用户抢控制权，联动行为更接近真实可用产品。
- 自动动作增加最小回滚后，联动失败时不至于立即把状态机拖成明显错误状态，后续定位问题更容易。

### 影响范围

- 核心实现文件：
  - `My_Project/linkage_logic.h`
  - `My_Project/linkage_logic.cpp`
  - `My_Project/tests/test_linkage_logic.cpp`
  - `My_Project/sensor_worker.h`
  - `My_Project/sensor_worker.cpp`
  - `My_Project/mainwindow.h`
  - `My_Project/mainwindow.cpp`
  - `My_Project/My_Project.pro`
- 文档同步文件：
  - `README.md`
  - `项目更新日志.md`
- 当前仍未改动的模块：
  - `Camera_Project` 的视频采集与显示链路
  - AP3216C / DHT11 / SR501 / LED 的内核驱动实现

### 当前状态

- 状态：核心代码已接入，待板端编译验收
- 已完成的本机验证：
  - `linkage_logic` 主机侧测试通过
  - 关键改动文件 `git diff --check` 通过
- 尚未完成的验证：
  - 由于当前工作机缺少 `qmake` / Qt 本机构建环境，本轮未完成 `My_Project` 的本机 Qt 编译验证
  - 仍需在板端完成交叉编译、部署与联动验收

### 覆盖关系

- 覆盖引用：`2026-03-22 更新 001：多传感器联动与多线程控制方案`
- 覆盖内容：覆盖该章节中“当前状态”里的“方案已确认，尚未实现”判断
- 覆盖原因：项目已经从 design-only 阶段推进到核心代码落地阶段，需要把状态从“方案”更新为“实现已接入、待验收”

## 2026-03-23 更新 003：板端启动与设备访问验收

### 变更内容

- 在开发板 `root@10.20.20.36` 上完成一次真实启动验证：
  - 手动加载 `led_drv.ko`、`ap3216c_drv.ko`、`dht11_drv.ko`、`sr501_drv.ko`
  - 确认 `/dev/led`、`/dev/ap3216c`、`/dev/dht11`、`/dev/sr501` 设备节点存在
  - 启动 `QT_QPA_PLATFORM=linuxfb ./My_Project`
- 启动后回读进程与文件描述符，确认 `My_Project` 实际打开了 `LED/AP3216C/DHT11/SR501` 四个字符设备，而不是只停留在“进程存在”的空启动状态。
- 使用 `ap3216cApp /dev/ap3216c` 追加验证传感器链路可读，实测可持续回读 ALS/PS/IR 数值。
- 当前板端实测快照：
  - `SR501` 当前读值为 `0`
  - `My_Project` 运行期间 `Camera_Project` 未被自动拉起
  - `AP3216C` 实测样本为 `ALS: 37889, PS: 32, IR: 0` 附近波动

### 直接好处

- 把“代码已接入”推进成“板端已能启动并接入真实设备”，减少后续把纯代码状态误判为整机可用的风险。
- 通过文件描述符回读可以直接证明 worker thread 已经进入真实设备访问阶段，不需要只靠主观推断。
- 先把设备加载、节点存在、程序常驻、传感器可读这四个基础条件锁定后，后续排查联动问题时可以更快收敛到“规则触发”而不是“底层没起来”。
- 形成一条可复用的板端验收基线，后续无论继续调 5 秒保持、60 秒冻结还是阈值，都可以先复跑这一组命令。

### 影响范围

- 运行验收对象：
  - `My_Project`
  - `led_drv.ko`
  - `ap3216c_drv.ko`
  - `dht11_drv.ko`
  - `sr501_drv.ko`
- 文档同步文件：
  - `README.md`
  - `项目更新日志.md`
- 当前仍未在本节内完成的验收：
  - `SR501` 实物触发摄像头自动启动与 5 秒自动停止
  - `AP3216C` 遮光后的自动控灯
  - 手动摄像头不被 auto-stop 误停
  - 手动 LED 后 60 秒自动控灯冻结

### 当前状态

- 状态：已完成板端启动与设备访问验收，待继续做“实物触发型联动验收”
- 本节已确认的事实：
  - 板端程序可启动并常驻
  - 四个字符设备节点存在且被 `My_Project` 实际打开
  - `AP3216C` 传感器当前可读
- 本节未声称已完成的事实：
  - 未声称 `SR501 -> Camera_Project` 的自动启停已经在本次运行中被实物触发验证
  - 未声称 `AP3216C -> LED` 的暗光自动开灯与亮光自动关灯已经在本次运行中被现场遮光验证
  - 未声称手动覆盖规则已经做过完整交互回归

### 覆盖关系

- 覆盖引用：`2026-03-23 更新 002：多传感器联动与多线程实现落地`
- 覆盖内容：覆盖该章节“当前状态”里的“待板端编译验收”表述
- 覆盖原因：本轮已经完成板端启动、设备节点、设备访问三项基础验收，状态应从“待板端编译验收”收紧为“已完成板端启动与设备访问验收，待实物联动验收”

## 2026-03-23 更新 004：SR501 自动启停摄像头板端实测

### 变更内容

- 在开发板持续运行 `My_Project` 的过程中，抓到了 `SR501 -> Camera_Project` 的自动启停日志链路：
  - `Linkage decision cached: ... startCamera= true stopCamera= false`
  - `Camera_Project` 成为 `My_Project` 的子进程
  - `Linkage decision cached: ... startCamera= false stopCamera= true`
  - `Camera_Project` 以 `QProcess::NormalExit` 退出
- 随后在同一轮运行中再次观察到：
  - `Linkage decision cached: ... startCamera= true stopCamera= false`
  - 说明自动启动链路在再次检测到人时能够重新触发，而不是只启动一次后失效
- 运行期间直接读取 `/dev/sr501`，连续样本为 `1 1 1`，说明现场当时确实存在人体感应高电平输入，不是纯软件假触发。

### 直接好处

- 把 `SR501 -> Camera_Project` 从“代码路径存在”推进到“板端真实触发过一次完整自动启停链路”，联动可信度显著提高。
- 通过 `My_Project -> Camera_Project` 的父子进程关系，可以直接排除“相机程序是手工单独启动”的歧义。
- 自动停止日志已经出现，说明状态机不仅会启动，还能走到回收路径；后续如果要继续盯 5 秒保持精度，只需加时间戳观测，不需要重新怀疑整条链路是否接通。
- 再次自动拉起也被观察到，说明联动不会在首轮触发后卡死在一次性状态。

### 影响范围

- 实测通过的链路：
  - `SR501 -> linkage_logic -> MainWindow/QProcess -> Camera_Project`
- 证据来源：
  - `My_Project` 板端标准输出日志
  - `ps -o pid,ppid,stat,comm,args` 进程树
  - `/dev/sr501` 连续读值
- 当前仍未在本节内完成的验收：
  - `5 秒` 保持时间的精确量测
  - `AP3216C -> LED` 的暗光/亮光自动切换
  - 手动启动摄像头不被 auto-stop 误停
  - 手动 LED 后 `60 秒` 自动控灯冻结

### 当前状态

- 状态：`SR501 -> Camera_Project` 自动启停链路已完成一次板端实测，剩余验收重点转为“时间精度 + AP3216C 控灯 + 手动覆盖规则”
- 本节已确认的事实：
  - `startCamera=true` 决策已在板端出现
  - `stopCamera=true` 决策已在板端出现
  - `Camera_Project` 已被 `My_Project` 自动拉起，并出现一次正常退出
- 本节未声称已完成的事实：
  - 未声称已经精确量测出“停止前恰好 5 秒”
  - 未声称 `AP3216C` 自动控灯和手动覆盖规则已完成板端实测

### 覆盖关系

- 覆盖引用：`2026-03-23 更新 003：板端启动与设备访问验收`
- 覆盖内容：覆盖该章节“当前仍未在本节内完成的验收”里关于 `SR501 -> Camera_Project` 自动启停尚未实物触发验证的表述
- 覆盖原因：同一轮板端运行中已经捕获到自动启动、自动停止和再次自动启动日志，原先的保守表述需要更新为“链路已实测，剩余待量化”

## 2026-03-24 更新 005：SR501 Qt 接线改为异步通知

### 变更内容

- 对照 `sr501_App.c` 与 `sr501_drv.c` 的实际用法，修正 `My_Project` 中 `SR501` 的 Qt 接线方式：
  - 不再在 `ap3216c_timeout()` 里顺带轮询读取 `/dev/sr501`
  - 改为启动时执行 `fcntl(F_SETOWN, getpid())` 和 `fcntl(F_SETFL, flags | O_ASYNC)`
  - 驱动发出 `SIGIO` 后，通过 `socketpair + QSocketNotifier` 把通知转进 Qt 主线程
  - 在 Qt 主线程里读取 `/dev/sr501` 并刷新 `label_people`
- 新增独立的 `sr501_async` helper，把异步通知配置、信号管道清空、设备状态读取拆成可单测的纯 C++ 小模块。
- 新增 `My_Project/tests/test_sr501_async.cpp`，先在主机侧验证 helper 的三个最小行为：
  - 能从 fd 读出当前人体状态
  - 能清空累计的信号管道字节
  - 能正确设置 `F_SETOWN` 和 `O_ASYNC`
- 同步修正 `README.md` 中把 `SR501` 描述成“worker thread 轮询”的表述，避免文档继续误导后续调试。

### 直接好处

- 和驱动真实机制对齐：驱动本身是 `fasync + kill_fasync(SIGIO)`，Qt 应用也改成同一条通知链路，后续排障不会再把“驱动异步”和“应用轮询”混为一谈。
- 降低无效读操作：`SR501` 只有在人体状态变化后才触发界面刷新，不再被 `AP3216C` 的 1 秒定时器绑着走。
- 便于后续重新接回联动：先把 `SR501 -> label_people` 这条基础通知链路做对，后面无论接回摄像头状态机还是多线程，都有稳定的异步入口可复用。
- 验证路径更清晰：现在既能回读远端 `sr501_App.c` 的参考用法，也能用本地最小测试先验证 helper，再上板验收。

### 影响范围

- 核心实现文件：
  - `My_Project/mainwindow.h`
  - `My_Project/mainwindow.cpp`
  - `My_Project/sr501_async.h`
  - `My_Project/sr501_async.cpp`
  - `My_Project/My_Project.pro`
  - `My_Project/tests/test_sr501_async.cpp`
- 文档同步文件：
  - `README.md`
  - `项目更新日志.md`
- 当前没有在本节内重新启用的能力：
  - `SR501 -> Camera_Project` 自动跳转
  - `AP3216C -> LED` 自动控灯
  - 之前那套多线程 `SensorWorker` 联动主流程

### 当前状态

- 状态：代码已修正，待本机测试与板端构建验收
- 本节已完成：
  - 已从 `ladykaka@10.20.41.20:/home/ladykaka/Linux/Linux_Drivers/copy_project/sr501_人体感应器/` 回读 `sr501_App.c` / `sr501_drv.c`
  - 已按该参考路径完成 `My_Project` 代码改造与 README 同步
- 本节尚未完成：
  - 本机执行 `test_sr501_async`
  - Ubuntu 远端 `qmake` 构建 `My_Project`
  - 开发板运行并确认 `label_people` 能随人体触发变化

### 覆盖关系

- 覆盖引用：`2026-03-23 更新 002：多传感器联动与多线程实现落地`
- 覆盖内容：覆盖该章节里“`SensorWorker` 轮询 `SR501`”以及 README 中“worker thread 轮询 AP3216C、DHT11、SR501”的表述
- 覆盖原因：当前 `release` 基线已经先回到“恢复基础功能、逐项接回机制”的收敛路径，而 `SR501` 驱动本身采用异步通知；继续把它记成轮询会误导后续实现和板端调试

## 2026-03-24 更新 006：My_Project 主界面回显与重绘修正

### 变更内容

- 针对“从 `Camera_Project` 返回后，`My_Project` 主界面文字和传感器数据显示空白”的症状，新增主界面恢复辅助逻辑：
  - 返回主界面时不再只做 `show()/raise()/activateWindow()`
  - 改为统一执行“重新全屏显示 + 立即刷新标签 + 强制重绘”
  - 增加两次延迟补刷，适配 `linuxfb` 下无窗口管理器场景
- 给 `My_Project` 主界面补了显式前景色/背景色，避免依赖板端默认调色板，降低“文字实际存在但颜色不可见”的风险。
- `ap3216c_timeout()` 增加了设备打开失败和读取失败的保护：
  - `fd < 0` 时直接把相关标签显示为 `--`
  - 读取长度异常时显示 `ERR`
  - 避免继续在失败路径上拿无效缓冲更新界面

### 直接好处

- 主界面从相机页返回后，会主动重刷整窗，而不是赌 framebuffer 自己恢复，这对 `linuxfb` 这种无窗口系统环境更稳。
- 即使板端默认主题或调色板变化，主界面文字也不再依赖默认配色，降低“看起来像空白页面”的风险。
- 传感器读取失败时会直接落到 `--/ERR`，后续现场调试时能区分“界面没重绘”和“设备没读到”两类问题。

### 影响范围

- 核心实现文件：
  - `My_Project/mainwindow.h`
  - `My_Project/mainwindow.cpp`
- 文档同步文件：
  - `项目更新日志.md`
- 当前没有在本节内触碰的模块：
  - `Camera_Project`
  - `driver_file/*`
  - 之前未提交的 `linkage_logic` / `sensor_worker` 残留文件

### 当前状态

- 状态：代码已修改，待远端构建恢复后重新编译，并在板端做返回主界面验收
- 本节已完成：
  - 完成显示恢复路径和显式样式的代码修改
  - `git diff --check` 通过
- 本节尚未完成：
  - 远端 `qmake` 构建；当前 `10.20.41.20:22` 连接超时
  - 板端“进入 Camera_Project 再退出回 My_Project”现场验收

### 覆盖关系

- 覆盖引用：`2026-03-24 更新 005：SR501 Qt 接线改为异步通知`
- 覆盖内容：不是覆盖其机制结论，而是在该基线之上补充主界面显示恢复修正
- 覆盖原因：`SR501` 异步通知已经接回，但主界面从相机页返回后的显示恢复仍有板端表现问题，需要单独记录这次 UI 重绘层面的修正，避免后续把它误判成传感器逻辑问题

## 2026-03-24 更新 007：My_Project 启动显示链路兜底修正

### 变更内容

- 继续收敛“主界面文字和传感器数据显示空白”的问题，把 `My_Project` 的启动显示链路改得更接近 `Camera_Project`：
  - 不再在 `MainWindow` 构造函数里直接 `show()`
  - 统一改到 `main.cpp` 里设置窗口标志并执行 `showFullScreen()`
  - 启动时加载 `Source Han Sans CN` 字体并设置应用默认字体
- 给 `centralwidget` 增加显式调色板和 `setAutoFillBackground(true)`，同时保留样式表，减少对板端默认主题的依赖。
- 所有关键传感器标签在构造阶段先初始化为 `--`，避免界面处于“未赋值时空白”的状态。

### 直接好处

- 避免构造函数里 `show()`、`main()` 里再次 `show()` 造成的重复显示路径，在 `linuxfb` 下更不容易触发异常回显。
- 文字显示不再依赖系统默认字体和主题，中文标题、按钮文本和数字标签都有更稳定的渲染基础。
- 即使设备首帧尚未刷新，主界面也会先显示占位值，而不是让用户看到一块没有文字的区域。

### 影响范围

- 核心实现文件：
  - `My_Project/main.cpp`
  - `My_Project/mainwindow.cpp`
  - `My_Project/mainwindow.h`
- 文档同步文件：
  - `项目更新日志.md`

### 当前状态

- 状态：代码已继续收敛，待远端构建机恢复后统一编译并上板验证
- 本节已完成：
  - 启动链路、字体、背景调色板、标签默认值的代码修改
  - `git diff --check` 通过
- 本节尚未完成：
  - 远端构建与板端显示验收

### 覆盖关系

- 覆盖引用：`2026-03-24 更新 006：My_Project 主界面回显与重绘修正`
- 覆盖内容：覆盖该章节只从“返回主界面后的重绘”角度修正问题的范围假设
- 覆盖原因：仅修返回重绘不足以排除“启动链路重复 show、字体缺失、默认调色板、未初始化标签”这些同样会表现为空白的因素，因此需要把问题收敛范围从“返回时重绘”扩大到“整个主界面显示链路”

## 2026-03-24 更新 008：默认启动方案切换为预加载脚本

### 变更内容

- 根据板端最新实测，把默认运行方案从“手工 `insmod` + 临时 `nohup` 启动”切换为“预加载脚本前台启动”：
  - 先加载 `led_drv/ap3216c_drv/dht11_drv/sr501_drv`
  - 再以前台 `exec env QT_QPA_PLATFORM=linuxfb ./My_Project` 运行
- 在仓库中新增 [scripts/preload_drivers.sh](/Users/apple/Embedded/projects/i.MX6ULL-Linux-V4L2-/scripts/preload_drivers.sh)，把这条路径固定下来。
- README 和 `scripts/README.md` 同步更新，后续默认指向这条脚本方案。

### 直接好处

- 启动链路变成可复用脚本，不再依赖当次 shell 环境或临时手敲命令。
- 固定使用 `/sbin/insmod`，避免板端 `PATH=/usr/bin:/bin` 时找不到 `insmod`。
- 固定前台运行 `My_Project`，与用户当前已验证可用的触摸/按钮路径一致，减少后台 `nohup` 方案带来的行为漂移。

### 影响范围

- 新增脚本：
  - `scripts/preload_drivers.sh`
- 文档同步文件：
  - `README.md`
  - `scripts/README.md`
  - `项目更新日志.md`

### 当前状态

- 状态：脚本与文档已改，待你按这条统一方案继续长期使用
- 本节已确认的事实：
  - 同一个 `My_Project` 二进制，在用户现有的 `preload_drivers.sh` 前台启动路径下，`SR501` 检测与按钮点击都正常
  - 板端 `/lib/modules/4.1.15-g3dc0a4b/My_Project` 与构建机产物 `md5` 一致

### 覆盖关系

- 覆盖引用：`2026-03-24 更新 007：My_Project 启动显示链路兜底修正`
- 覆盖内容：覆盖此前把“我的临时手工启动命令”默认当成运行基线的做法
- 覆盖原因：用户给出的新证据表明，同一个二进制在“预加载驱动 + 前台启动”路径下表现更稳定，因此默认方案应当切换到这条已实测路径

## 2026-03-24 更新 009：远程启动命令改为 bash 登录壳

### 变更内容

- 继续修正“默认远程启动命令”的写法，不再把远程基线写成：
  - `ssh root@10.20.20.36 'cd /lib/modules/4.1.15-g3dc0a4b && ./preload_drivers.sh'`
- 当前默认远程启动命令改为：
  - `ssh root@10.20.20.36 "bash -l -c 'cd /lib/modules/4.1.15-g3dc0a4b && ./preload_drivers.sh'"`
- README 与 `scripts/README.md` 已同步改写为这条命令。

### 直接好处

- 远程执行环境更接近“板端手动登录后再执行 `./preload_drivers.sh`”的现场路径。
- 避免把“SSH 非 login shell”和“板端本地前台 shell”混成同一种环境，减少由 shell 初始化差异带来的误判。
- 后续远程验证按钮或触摸时，至少先和用户手工路径保持同一层级的 shell 环境。

### 影响范围

- 文档同步文件：
  - `README.md`
  - `scripts/README.md`
  - `项目更新日志.md`
- 本节没有改动：
  - `My_Project` 二进制
  - 板端现有 `preload_drivers.sh` 的最小逻辑

### 当前状态

- 状态：文档已更新，远程默认命令已切换
- 本节已确认的事实：
  - 用户指出 `ssh root@10.20.20.36 "bash -l -c 'cd /lib/modules/4.1.15-g3dc0a4b && ./preload_drivers.sh'"` 可以执行
  - 之前直接 `ssh 'cd ... && ./preload_drivers.sh'` 与板端手工执行不等价

### 覆盖关系

- 覆盖引用：`2026-03-24 更新 008：默认启动方案切换为预加载脚本`
- 覆盖内容：覆盖该章节里“远程默认运行方案”的具体命令表达
- 覆盖原因：新的板端证据表明，远程场景下还需要补上 `bash -l -c` 这一层，才能更接近用户本地手工启动环境

## 2026-03-23 更新 005：自动会话返回主界面与相机内按钮接管修正

### 变更内容

- 修正 `My_Project` 在自动摄像头会话结束后的收尾逻辑：
  - 之前只有“手动启动”的 `Camera_Project` 退出后才会重新显示 `My_Project`
  - 现在只要相机会话真正启动过，并且当前不是 shutdown/restart 过程，结束后都会恢复 `My_Project` 全屏界面
- 为 `Camera_Project -> My_Project` 增加最小交互信号：
  - 用户点击 `拍照` 时，`Camera_Project` 输出一条显式的 `CAMERA_EVENT USER_INTERACTION CAPTURE`
  - 用户点击 `退出` 时，`Camera_Project` 输出一条显式的 `CAMERA_EVENT USER_INTERACTION EXIT`
- `My_Project` 侧新增子进程输出解析：
  - 自动会话中只要收到上述交互标记，就把当前摄像头会话从 `Auto` 升级为 `Manual`
  - 升级后，该会话不再受 `SR501` 的 5 秒 auto-stop 直接关闭
- 为降低误判风险，新增纯 C++ helper 测试：
  - “自动会话被用户接管后不再被 auto-stop 关闭”
  - “子进程真正结束后应恢复主界面”
  - “只有显式 `CAMERA_EVENT USER_INTERACTION ...` 才会触发接管”

### 直接好处

- 直接修复了“自动停止后停在 `Camera_Project` 最后一帧，回不到 `My_Project`”这个板端可见问题。
- 把“用户在相机界面已经开始操作”正式纳入状态机，而不是继续按“无人且超时就强退”处理，交互更符合实际使用预期。
- 采用标准输出标记而不是新增复杂 IPC，改动面小，容易在现有 `QProcess` 结构上验证和维护。
- 新增纯逻辑测试后，后续如果再改会话 ownership 或返回逻辑，主机侧能先挡住回归。

### 影响范围

- 核心实现文件：
  - `My_Project/linkage_logic.h`
  - `My_Project/linkage_logic.cpp`
  - `My_Project/tests/test_linkage_logic.cpp`
  - `My_Project/mainwindow.h`
  - `My_Project/mainwindow.cpp`
  - `Camera_Project/mainwindow.cpp`
- 文档同步文件：
  - `README.md`
  - `项目更新日志.md`
- 当前仍未在本节内完成的验收：
  - 板端真实点击 `Camera_Project` 的拍照/退出按钮，确认“接管后不会被 auto-stop 打断”
  - 5 秒保持时间的精确量测
  - `AP3216C -> LED` 的暗光/亮光自动切换
  - 手动 LED 后 `60 秒` 自动控灯冻结

### 当前状态

- 状态：已完成主机侧单元测试、Ubuntu 远端构建、板端部署；待板端真实点击验收
- 本节已确认的事实：
  - `linkage_logic` 新增测试已通过
  - `My_Project` 与 `Camera_Project` 已完成 Ubuntu 远端 qmake 构建
  - 新版 `My_Project` 已手工替换部署到开发板
- 本节未声称已完成的事实：
  - 未声称本节的“拍照/退出后升级为手动接管”已经在板端真实点击验证通过
  - 未声称本节的“自动结束后恢复主界面”已经在当前这轮无人体触发场景下做过现场画面确认

### 覆盖关系

- 覆盖引用：`2026-03-23 更新 004：SR501 自动启停摄像头板端实测`
- 覆盖内容：覆盖该章节剩余验收项里“手动启动摄像头不被 auto-stop 误停”的旧表述边界，把它扩展为“自动拉起后的用户接管会话也不应被 auto-stop 误停”
- 覆盖原因：用户反馈当前交互设计不符合实际使用场景，需要把“用户已在 Camera_Project 内开始操作”的会话从自动路径提升到手动路径，这是交互设计修正，不是单纯参数微调

## 2026-03-24 更新 010：移除 My_Project 假空气值占位显示

### 变更内容

- 回查 `My_Project/mainwindow.ui` 后确认：
  - 主界面“空气”这一组标签没有对应的真实传感器接线
  - 数值 `300` 是直接写死在 UI 里的静态占位值，不会被业务代码刷新
- 因此删除主界面中的两项占位控件：
  - `空气:` 标题标签
  - 默认显示 `300` 的数值标签

### 直接好处

- 避免把静态占位值误看成真实传感器数据，界面语义更准确。
- 后续做板端联调时，屏幕上看到的项目都能对应到实际设备节点，排障更直接。
- 把没有真实数据源的 UI 元素先移除，可以减少后续演示和文档说明中的歧义。

### 影响范围

- 核心实现文件：
  - `My_Project/mainwindow.ui`
- 文档同步文件：
  - `项目更新日志.md`

### 当前状态

- 状态：已完成本地 UI 清理，待随本次仓库改动统一提交
- 本节已确认的事实：
  - `空气` 标签对应的 `300` 来自 UI 静态文本，不是运行时传感器读值
  - 业务代码中不存在 `label_air` 的刷新逻辑

### 覆盖关系

- 覆盖引用：`2026-03-23 更新 002：多传感器联动与多线程实现落地`
- 覆盖内容：覆盖该阶段主界面里遗留的“无真实数据源占位显示仍保留在 UI 中”的旧状态
- 覆盖原因：这是界面语义修正。当前项目没有空气传感器输入链路，继续保留固定 `300` 只会误导联调和演示

## 2026-03-24 更新 011：移除 My_Project 假门铃按钮占位

### 变更内容

- 回查 `My_Project/mainwindow.ui` 和 `mainwindow.cpp/.h` 后确认：
  - 主界面 `门铃` 按钮对应的是 `pushButton_2`
  - 代码中没有 `on_pushButton_2_clicked()`，也没有门铃设备访问或业务处理逻辑
- 因此删除主界面中的 `门铃` 按钮控件，避免继续保留无功能占位按钮

### 直接好处

- 避免用户误以为板端已经接了门铃功能，界面表达更准确。
- 减少无效交互入口，演示和联调时不会再点到一个没有任何动作的按钮。
- 主界面只保留当前真实可用的功能入口，后续继续扩展时边界更清楚。

### 影响范围

- 核心实现文件：
  - `My_Project/mainwindow.ui`
- 文档同步文件：
  - `项目更新日志.md`

### 当前状态

- 状态：已完成本地 UI 清理，待重新编译并部署到板端
- 本节已确认的事实：
  - `门铃` 按钮在当前版本中没有槽函数接线
  - 当前项目中不存在门铃设备驱动或 Qt 侧门铃控制链路

### 覆盖关系

- 覆盖引用：`2026-03-23 更新 002：多传感器联动与多线程实现落地`
- 覆盖内容：覆盖该阶段主界面里遗留的“无真实功能入口仍保留在 UI 中”的旧状态
- 覆盖原因：这是界面语义修正。当前项目没有门铃功能实现，继续保留该按钮只会误导使用者

## 2026-03-24 更新 012：多传感器联动第一阶段重新启动

### 变更内容

- 由于之前联动方案在板端叠加后出现难以继续收敛的问题，本轮从当前稳定 git 基线重新启动多传感器联动。
- 这次明确采用“第一阶段最小可用方案”：
  - 保留当前 `SR501` 异步通知链路
  - 保留当前 `AP3216C/DHT11` 定时器读取链路
  - 不恢复历史上的 `SensorWorker/QThread + 自动/手动 ownership` 整包方案
- 在当前 `My_Project/mainwindow.h/.cpp` 中重新打开两条自动动作：
  - `SR501 -> Camera_Project`
  - `AP3216C -> LED`
- 同步新增本轮文档链路：
  - `联动逻辑.md`
  - `docs/plans/2026-03-24-sensor-linkage-restart-design.md`
  - `docs/plans/2026-03-24-sensor-linkage-restart.md`
  - `docs/plans/2026-03-24-sensor-linkage-restart-execution-log.md`

### 直接好处

- 把联动恢复范围收紧到“当前稳定基线上的自动动作重新打开”，避免再次把线程、接管逻辑、UI 交互问题混在一起排障。
- `SR501` 继续使用已经稳定的异步通知链路，避免为了恢复自动动作又把驱动接线改回轮询。
- `AP3216C` 自动控灯直接复用当前定时器读取路径，改动面小，便于板端回归。
- 新增设计/计划/执行日志三件套后，后续每一步实现、阻塞和验证都有文档证据，不再只散落在聊天记录里。

### 影响范围

- 核心实现文件：
  - `My_Project/mainwindow.h`
  - `My_Project/mainwindow.cpp`
- 新增文档：
  - `联动逻辑.md`
  - `docs/plans/2026-03-24-sensor-linkage-restart-design.md`
  - `docs/plans/2026-03-24-sensor-linkage-restart.md`
  - `docs/plans/2026-03-24-sensor-linkage-restart-execution-log.md`

### 当前状态

- 状态：第一阶段代码接线、远端构建、板端部署与现场联动验收均已完成
- 本节已确认的事实：
  - `linkage_logic` 主机侧测试当前可通过
  - `bash scripts/remote_qt_build.sh My_Project --skip-deploy` 当前可通过
  - 当前 `My_Project` 已重新接入第一阶段自动动作代码
  - 板端现场反馈：
    - `SR501`: 自动打开，`5 秒` 后关闭
    - `AP3216C`: 遮挡亮灯，移开灭灯
- 本节未声称已完成的事实：
  - 未声称第二阶段线程化联动方案已经恢复
  - 未声称自动/手动会话接管逻辑已经恢复

### 覆盖关系

- 覆盖引用：`2026-03-23 更新 002：多传感器联动与多线程实现落地`
- 覆盖内容：覆盖该章节“继续沿原线程化联动方案推进”的默认延续路径
- 覆盖原因：之前的整包联动方案在板端叠加后调试成本过高，本轮改为“从当前稳定基线重新启动第一阶段自动动作”，这是稳定性优先的实现路径调整
